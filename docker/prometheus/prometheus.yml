global:
  scrape_interval: 15s

scrape_configs:
  - job_name: caremesh-api
    metrics_path: /metrics
    static_configs:
      # The API runs on the host during dev.
      - targets: ["host.docker.internal:8000"]
